package _09.lambda;

//@FunctionalInterface -> it's a functional interface, have a one method, don't have to declare functional interface
public interface Operation {

    int operate(int x, int y);
}
